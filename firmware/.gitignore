# 리포에 넣지 않는 것. 파일은 이 폴더에 그대로 두고 추적만 안 한다.
# gitignore는 한 줄 안의 뒷주석을 패턴의 일부로 읽으므로 주석은 따로 쓴다.

# Arduino 빌드 산출물, 약 27MB
build/

# NUCODE 제공 예제. 우리 코드가 아니고 보드 패키지에서 다시 받을 수 있다
vendor/

# NU40-DK 매뉴얼·회로도 PDF, 약 7MB
references/

# 빌드 결과 압축본
*.zip
