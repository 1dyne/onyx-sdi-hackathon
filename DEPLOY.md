# Onyx SDI — 배포 및 현장 운영

## 배포할 때 (코드를 고쳤다면 반드시)

```bash
python bump-version.py 2.0.1
```

그 다음 커밋 & 푸시하면 끝입니다. 기기에서는 **새로고침 1회**로 반영됩니다.

버전을 올리지 않고 푸시하면 구버전이 뜰 수 있습니다. 이 스크립트가 두 곳을 동시에 갱신하기 때문에 손으로 파일을 고치지 마세요.

| 갱신 대상 | 무엇을 무효화하는가 |
|---|---|
| `js/version.js`의 `BUILD_VERSION` | Service Worker URL(`sw.js?v=…`) → 새 워커 설치 + 구버전 캐시 삭제 |
| `index.html`의 모든 `?v=` | 브라우저 자체 HTTP 캐시 (GitHub Pages는 `max-age=600`을 보냄) |

반영 여부는 **헤더 좌측의 버전 표시**로 확인합니다. 숫자가 안 바뀌었으면 아직 구버전입니다.

## 캐시가 말썽일 때 (행사 당일)

우선순위 순으로:

1. **URL에 `?nosw=1`** — Service Worker를 등록하지 않고 캐시를 전부 지웁니다. 헤더에 노란 `DEV` 배지가 뜹니다. 한 번 붙이면 계속 유지됩니다.
2. **CONFIG → 앱/캐시 → 개발 모드** — 위와 동일, 스위치로 조작.
3. **CONFIG → 앱/캐시 → 캐시 삭제 후 새로고침** — 1회성 강제 초기화.

정상 모드로 돌아오려면 `?nosw=0` 또는 개발 모드 스위치 off.

> 개발 모드에서도 저장된 동작·세션 기록(localStorage)은 지워지지 않습니다.

## 유닛 연결

버튼으로 좌우를 **사용자가 지정**합니다. 기기 이름으로 자동 판별하지 않으므로 펌웨어 이름이 무엇이든 상관없습니다.

- 헤더의 `L` / `R` 칩 — 어디서나 연결/해제
- LIVE 탭 상단 카드 — `왼발` / `오른발` 전체 버튼

**한쪽만 연결해도 모든 기능이 정상 동작합니다.** 미연결 쪽은 흐리게 표시되고 게이지는 `---`, 좌우 비교 위젯만 비활성화됩니다. 세션 도중에 반대쪽을 연결하면 그 시점부터 합류하며, 합류 시각이 로그에 기록됩니다.

한쪽이 끊기면 자동으로 3회까지 재연결을 시도합니다. 다른 쪽은 영향받지 않습니다.

## 문제가 생기면 — CONFIG → 진단

**RAW 모니터**를 먼저 여세요. 대부분 한 줄만 보면 원인이 나옵니다.

| 증상 | 원인 | 조치 |
|---|---|---|
| `수신 데이터 없음` | 연결은 됐지만 notify가 안 옴 | 펌웨어 전송 루프 확인 |
| `FSR 범위 … ⚠ 1023 초과` | 해당 유닛이 12-bit ADC | 진단 → ADC 최대값을 `4095`로 |
| `필드 개수 5 (7 또는 4 필요)` | 구분자/필드 수 불일치 | 펌웨어 출력 포맷 확인 |
| `필드 3 숫자 아님: "abc"` | 해당 필드가 깨짐 | 표시된 필드 번호만 확인 |
| Hz가 0 또는 비정상 | 전송 주기 문제 | 펌웨어 delay 확인 |
| 기기 선택창에 안 뜸 | 광고 이름/서비스 불일치 | `연결 시 모든 BLE 기기 표시` 켜기 |
| 다른 프로필로 바뀜 | NUS가 아님 | `커스텀 서비스 UUID`에 입력 (코드 수정 불필요) |

## 하드웨어 없이 검증하기

콘솔에서:

```js
bluetooth.left.startSimulation(store.getDrill('<drillId>'));
bluetooth.right.startSimulation(store.getDrill('<drillId>'));
```

시뮬레이션은 **실제 수신 경로를 그대로 통과**합니다 — 파싱, ADC 환산, 주기 측정, RAW 모니터가 하드웨어와 동일하게 동작하므로 UI 검증에 그대로 쓸 수 있습니다.

## 알아둘 것

- **YAW는 참고용입니다.** 양쪽 다 6축 IMU(LSM6DS3TR-C)라 지자기가 없어 자이로 적분 드리프트가 누적됩니다. IMU 카드에 마지막 캘리브레이션 후 경과 시간이 표시되고, 3분이 넘으면 노란색으로 바뀝니다. `⊕ CAL`은 발마다 따로 누릅니다.
- **좌우 비교는 roll 기준이 신뢰도가 높습니다.** roll/pitch는 가속도계로 보정되어 드리프트가 없습니다.
- **경고음은 5종 그대로**입니다. 왼발은 좌측 채널, 오른발은 우측 채널 + 약간 높은 음으로 재생됩니다. 스피커(모노)에서도 음 높이로 구분됩니다. CONFIG → AUDIO에서 `▶ L` / `▶ R`로 확인하세요.
- **iOS는 Web Bluetooth 미지원**입니다. 실측은 안드로이드 크롬에서만 됩니다. iOS에서는 기록 열람만 가능합니다.
- **기준값(REF)은 좌우 따로** 저장됩니다. `◎ REF SAVE`는 그 시점에 연결된 발만 저장하며, 반대쪽 기존 값은 건드리지 않습니다.
